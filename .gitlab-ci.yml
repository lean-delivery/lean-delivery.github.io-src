---
stages:
  - validate
  - deploy

#variables:
#  GET_SOURCES_ATTEMPTS: "5"

job1:
  stage: validate
  before_script:
    - pip install rstvalidator
    - pip install pelican
    - sed -i 's/lean-delivery.com/preview.lean-delivery.com/g' pelicanconf.py
  script:
    - python -m rstvalidator content/articles/
    - python -m rstvalidator content/pages/
  after_script:
    - pelican -s publishconf.py
  tags:
    - aws

job2:
  stage: deploy
  script:
    - aws s3 cp output/* s3://blog-dev-blog-origin/$CI_COMMIT_REF_NAME/ --recursive
  tags:
    - aws